key=[]
value=[]
with open (fasta, "r") as file:
    for lines in file:
        if not lines.startswith(">"):
            key.append(lines.split('.')[0])
        else:
            value.append(lines)


dictionary = dict(zip(key, value))
print(dictionary)
